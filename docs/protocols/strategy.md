# Protocol Strategy

All external systems integrate through explicit vendor-neutral contracts.

Vendor-specific adapters remain isolated behind integration boundaries.

Vendor-specific implementation details must not leak into the core ORBITA domain model.

Contracts must be versioned and compatibility must be explicit.
